Small, generic UI primitives with no task-specific logic: `Chip`,
`IconButton`, `ProgressBar`, etc. If a component only makes sense in the
context of a task, it belongs in `components/task/` instead.
